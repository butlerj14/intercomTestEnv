<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome <%= user %></title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet"> <!-- Add Montserrat font -->
    <style>
        body {
            font-family: 'Montserrat', sans-serif; /* Apply Montserrat font */
            color: #555555; /* Default text color */
            background-color: #f4f4f4; /* Light background color for better contrast */
        }

        h1 {
            color: #35DF8C; /* Secondary color for headings */
        }

        a {
            color: #35DF8C; /* Secondary color for links */
            text-decoration: none;
        }

        a:hover {
            color: #555555; /* Accent color on hover */
        }

        button {
            background-color: #555555; /* Accent color for buttons */
            color: white;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
            font-size: 16px;
            border-radius: 5px;
        }

        button:hover {
            background-color: #35DF8C; /* Secondary color on hover */
        }

        form {
            max-width: 400px;
            margin: 0 auto;
            padding: 20px;
            background-color: white;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
        }

        label {
            display: block;
            margin-bottom: 10px;
            color: #555555;
        }

        input[type="text"],
        input[type="password"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }
    </style>
</head>
<body>
    <header>
        <img src="https://drive.google.com/file/d/1ViwUfeLdoNnrIehX1sVz1_AzwBPw_USQ/view?usp=sharing" alt="Company Logo" style="width: 150px;">
    </header>
    <main>
        <h1>Welcome, <%= user %>!</h1>
        <a href="/logout">Logout</a>
    </main>

    <!-- Intercom Messenger Script -->
    <script>
        window.intercomSettings = {
            api_base: "https://api-iam.intercom.io",
            app_id: "<%= process.env.INTERCOM_APP_ID %>",
            user_id: "<%= email %>",
            user_hash: "<%= intercomHash %>"
        };
    </script>

    <script>
        (function() {
            var d = document;
            var s = d.createElement('script');
            s.type = 'text/javascript';
            s.async = true;
            s.src = 'https://widget.intercom.io/widget/<%= process.env.INTERCOM_APP_ID %>';
            var x = d.getElementsByTagName('script')[0];
            x.parentNode.insertBefore(s, x);
            s.onload = function() {
                Intercom('boot', window.intercomSettings);
            };
        })();
    </script>    
</body>
</html>
