<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet"> <!-- Add Montserrat font -->
  <style>
      body {
          font-family: 'Montserrat', sans-serif; /* Apply Montserrat font */
          color: #555555; /* Default text color */
          background-color: #555555; /* Light background color for better contrast */
      }

      h1 {
          color: #35DF8C; /* Secondary color for headings */
      }

      a {
          color: #35DF8C; /* Secondary color for links */
          text-decoration: none;
      }

      a:hover {
          color: #555555; /* Accent color on hover */
      }

      button {
          background-color: #555555; /* Accent color for buttons */
          color: white;
          border: none;
          padding: 10px 20px;
          cursor: pointer;
          font-size: 16px;
          border-radius: 5px;
      }

      button:hover {
          background-color: #35DF8C; /* Secondary color on hover */
      }

      form {
          max-width: 400px;
          margin: 0 auto;
          padding: 20px;
          background-color: white;
          box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
          border-radius: 8px;
      }

      label {
          display: block;
          margin-bottom: 10px;
          color: #555555;
      }

      input[type="text"],
      input[type="password"] {
          width: 100%;
          padding: 10px;
          margin-bottom: 20px;
          border: 1px solid #ccc;
          border-radius: 4px;
      }
  </style>
</head>
<body>
  <header>
    <img src="/images/SaaSyLogo.png" alt="Company Logo" style="width: 150px;">
  </header>
  <main>
      <h1>Login</h1>
      <form action="/login" method="POST">
        <label for="username">Username:</label>
        <input type="text" name="username" id="username" required>
        <label for="password">Password:</label>
        <input type="password" name="password" id="password" required>
        <button type="submit">Login</button>
      </form>
  </main>
</body>
</html>
